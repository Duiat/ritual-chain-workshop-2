# Title block

I file a sheet (`createMarket`). That call also enlists the wake. No second tx, no resolve button.

Ink: `HAIRLINE` 0.004 RITUAL, `OVERDRAWN` 25. A line thinner than that is `Hairline`, not `ZeroStake`. A fat splash is `Overdrawn`.

The loft does not keep a bell. Scheduler nudges `onScheduledResolve`. HTTP `0x0801` then hatch `0x0803`. Three muted gets and the sheet is void — we do not invent a NO.

Helpers I actually used: `spanFor`, `ruleFits`, `holdUntil`, `liveSheets`.
